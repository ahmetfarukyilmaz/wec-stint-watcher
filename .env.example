# Caddy reverse proxy domain (DNS A record must point to this server, DNS-only / no CDN proxy).
# No real domain? Use sslip.io: <dashed-ip>.sslip.io resolves to the IP automatically.
DOMAIN=timing.thefcompass.com

# Optional: email for Let's Encrypt expiry notices.
ACME_EMAIL=
